console.log("Update JS file executed");
// Update the individual product total.
$("#product-total-<%= @cart_item.id %>").text("<%= j number_to_currency(@cart_item.calculate_price) %>");

// Update the cart total.
$("#total-price").text("<%= j number_to_currency(cart_total) %>");

// Update the cart item count in the shopping cart icon
$("#cart-item-count").text("<%= j current_user.cart.total_items %>");

// Update the modal content
$('#shoppingCartModal .modal-body').html('<%= j render partial: "layouts/cart_items_list", locals: { current_cart: current_cart } %>');
